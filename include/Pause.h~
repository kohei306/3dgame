#ifndef PAUSE_H_
#define PAUSE_H_

#include "Base.h"
class Play;

class Pause : public Base
{
 public:
  //update function for Play class
  Base *update(Play* play);

  //keybord handler
  void keybord_handler(unsigned char key);

 
};

#endif
